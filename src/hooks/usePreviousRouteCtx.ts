import { useContext } from 'react';
// import { PerviousRouteCtx } from '@/pages/_app';

const usePreviousRouteCtx = () => {
  // const ctx = useContext(PerviousRouteCtx);
  return;
};

export default usePreviousRouteCtx;
