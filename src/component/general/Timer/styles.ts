import { Button, styled } from '@mui/material';

const TimerButton = styled(Button)(`
    height: 44px;
    padding: 14px;
`);

export const ShowTimerButton = styled(Button)(`
    width: 70px;
`);

export default TimerButton;
