version: "3.9"

services:
  tensurf-ui:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "3000:3000"
    networks:
      - tensurf_network

networks:
  tensurf_network:
    name: tensurf_network

